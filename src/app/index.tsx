import React from "react";
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Index = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-blue-500 text-center">Anil Tanriverdiler</Text>
      </View>
    </SafeAreaView>
  );
};

export default Index;
